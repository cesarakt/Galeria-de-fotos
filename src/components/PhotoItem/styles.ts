import styled from 'styled-components'

export const Container = styled.div`
  background: var(--grey);
  border-radius: 0.75rem;
  padding: 1rem;
  text-align: center;

  img {
    max-width: 100%;
    display: block;
    margin-bottom: 1rem;
    border-radius: 0.75rem;
  }

  span {
    font-size: 1.25rem;
  }
`
